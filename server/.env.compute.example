# Cloud server SSH connection
CLOUD_SSH_HOST=api.beyondlda2.com
CLOUD_SSH_USER=root
CLOUD_SSH_PORT=22
CLOUD_SSH_KEY=/home/user/.ssh/id_ed25519

# PostgreSQL password (must match cloud server's DB_PASSWORD)
DB_PASSWORD=beyondlda2

# Worker resources
MPI_NPROCS=4
WORKER_CPUS=4
WORKER_MEM=4g
