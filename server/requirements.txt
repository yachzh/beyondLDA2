# === API Server ===
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0

# === Task Queue ===
celery>=5.3.0
redis>=5.0.0

# === CORS ===
# (FastAPI CORSMiddleware is built-in; no extra dep needed)

# === CORE ===
# beyondLDA2 + GPAW + ASE are expected to be installed separately
# (they require compiled extensions and PAW datasets)
# numpy >=1.22
